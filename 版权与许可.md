# 版权与许可清单

> **结论：目前网站上不存在任何第三方素材的版权风险。**
> 字体只用系统中已有的（网站不分发任何字体文件）、图标是自己手写的 SVG、
> 唯一的第三方代码是 KaTeX（MIT，许可证原文已随站点一起发布）。
> 下面是逐项检查结果与证据，方便你以后加东西时对照。

---

## 一、字体：不分发字体文件，零风险

| 检查项 | 实测结果 |
| --- | --- |
| 站内有没有字体文件 | 只有 KaTeX 数学公式用的 20 个 `.woff2`（属于 KaTeX，MIT，见第三节） |
| CSS 里有没有 `@font-face` | **0 个** —— 说明本站没有自托管任何字体 |
| 正文和标题用什么字体 | 只写"字体名"交给系统去找：思源宋体 / Noto Serif SC / 宋体 / SimSun / Georgia 等 |

在 CSS 里写 `font-family: "Songti SC", SimSun, serif`，意思是"请浏览器用这台电脑里装的这个字体"，
**不涉及复制和分发**，因此不存在字体授权问题。真正的风险只出现在把字体文件打包进网站的时候——本站没有这样做。

**以后想换更漂亮的字体**：优先选 **SIL Open Font License（OFL）**，它明确允许网页嵌入与再分发。
中文推荐 [思源宋体 / Source Han Serif](https://github.com/adobe-fonts/source-han-serif)（OFL）
或 Noto Serif SC（OFL）。**不要**把微软雅黑、苹方、方正、汉仪等商业字体嵌进网页。

---

## 一点五、地球的海岸线数据：公共领域

首页那颗地球上的七大洲轮廓来自 **Natural Earth**（`ne_110m_land`，1:110m 简化版），Natural Earth 数据属于**公共领域（Public Domain）**，可自由使用、无需署名。
本项目把它下载后大幅简化（5143 个点 → 约 1200 个点），存成 `themes/map/assets/js/world-data.js`，生成脚本是 `.tools/fetch-world.cjs`。

## 二、图标与插画：全部是手写的 SVG

`themes/map/layouts/partials/icon.html` 里的三十多个图标（用户、羽毛笔、Σ、叶子、锅、锁、钥匙、邮件……）
全部是手写的 `<path>` 数据，**没有引入任何图标库**（Font Awesome、Material Icons、iconfont 都没有用）。
→ 不存在第三方授权问题。

首页主视觉那幅「星空 + 地球 + 三个入口」同样是手写 SVG
（`themes/map/layouts/partials/hero-scene.html`）：星野坐标、地球弧线、星星/彩虹/潮汐三个图标
都是为本站单独画的，**没有使用任何第三方插画、纹理或素材图片**。纸张纹理是内联的 SVG 噪声滤镜，
`static/favicon.svg` 也是自己画的。

---

## 三、KaTeX（数学公式渲染）：MIT，许可证已随站分发

| 项目 | 内容 |
| --- | --- |
| 用了什么 | `katex.min.js`、`katex.min.css`、`auto-render.min.js`、`mhchem.min.js` + 20 个数学字体 |
| 版本 | 0.18.7（官方 npm 包 `dist/` 原样拷贝） |
| 许可证 | **MIT**（包的 `package.json` 里 `"license": "MIT"`，Copyright (c) 2013-2020 Khan Academy and other contributors） |
| 是否满足分发要求 | **是**：许可证原文放在 `themes/map/static/vendor/katex/LICENSE`，会随站点一起发布（线上可访问 `/vendor/katex/LICENSE`），另附 `SOURCE.txt` 说明来源与版本 |
| 为什么放在本地 | 不依赖 CDN：国内访问稳定，也不会把访客 IP 暴露给第三方 CDN |

MIT 允许商用、修改、再分发，唯一要求是"保留版权声明与许可证"——已经做到。

> 关于 KaTeX 的数学字体：它们由 KaTeX 项目以同一 MIT 许可证分发
> （字体源流是 Donald Knuth 的 Computer Modern 与 AMS 字体，本身也是可自由使用的开放字体）。

---

## 四、主题与样式：自己写的

- 主题 `themes/map/` 是从零写的，**没有基于任何现成的 Hugo 主题**
- 代码高亮配色 `assets/css/chroma.css` 是按 CSS 变量自己写的，没有复制别人的配色文件
- 纸纹背景是内联的 SVG 噪声滤镜；`static/favicon.svg` 自己画的
- 用到的字体名、颜色值、尺寸数值属于事实性信息，不受版权保护

---

## 五、外部请求：一个都没有

| 检查项 | 实测结果 |
| --- | --- |
| 模板里出现的外部 `src` / `href` | 只有 `https://gohugo.io`（页脚的一个文字链接，不加载任何资源） |
| 有没有从 CDN 加载 CSS / JS / 字体 | **没有** |
| 有没有统计、广告、追踪代码 | **没有** |

好处：页面不会向任何第三方发请求 → 访客 IP 不会泄露给 CDN，也不会因为某个 CDN 被墙而导致样式丢失。

---

## 六、工具链：只在本地或构建时使用，不随站点分发

| 工具 | 许可证 | 是否随站点分发 |
| --- | --- | --- |
| Hugo 扩展版 v0.166.0 | Apache-2.0 | 否（`.tools/hugo/` 已被 git 忽略；GitHub Actions 每次构建时现场下载） |
| Node.js（写作台的运行环境） | MIT | 否 |
| Pages CMS（可选的网页写作后台） | 开源 | 否，它只是远程读写你的 GitHub 仓库 |

---

## 七、你自己的内容

`content/` 下的文章、你上传的照片，版权都归你所有。两点提醒：

1. **照片请用自己拍的**。别人的图、网上下载的图、动画截图，即使只是放在"创作记录"里也有风险。
2. **同人作品要遵守原作的同人规约**（多数日本版权方有明确规约，例如禁止商用、需标注原作）。
   这不影响网站本身，但影响你发布的内容。

---

## 八、以后想加新东西时的快速判断

| 想加什么 | 判断标准 |
| --- | --- |
| 网页字体 | 选 OFL 的（思源宋体、Noto 系列、霞鹜文楷等）；商业中文字体不要嵌入 |
| 图标 | 自己画，或用 MIT / OFL / CC0 的图标集，并保留其 LICENSE 文件 |
| 图片 | 自己拍的最稳；用图库要看清授权（能否商用、是否需署名） |
| JS 库 | 看 LICENSE：MIT / Apache-2.0 / BSD 都可以；GPL 要谨慎；来源不明的不要用 |
| 音乐、视频 | 尽量嵌入官方播放器，不要把文件搬到自己站点上 |

---

*最后更新：2026-09-20。以上检查项均通过命令实测（字体文件清单、`@font-face` 计数、
模板外链扫描、KaTeX 包内 LICENSE 与 `package.json` 的 license 字段核对）。*
