# 写作指南 · 怎么更新网站内容

## 先理解这条流水线

```
你写的文字（content/ 里的文章）
      ↓  Hugo 把它变成网页
    public/ 里的静态网页
      ↓  发布到 GitHub
   GitHub Actions 自动构建并上线
      ↓
https://mysteriousamateurplayer.github.io/Parlour/
```

**所以：更新网站 = 写文章。** 你完全不需要写任何代码。三种写法，挑一种顺手的：

| 方式 | 适合 | 需要什么 | 多久生效 |
| --- | --- | --- | --- |
| **① 写作台**（推荐，最省事） | 填表一样写文章、传照片，所见即所得 | 双击 `写作台.bat` | 点「保存并发布」后 1~2 分钟 |
| **② 网页后台**（Pages CMS） | 在手机或别的电脑上写 | GitHub 账号 + 浏览器 | 点 Save 后 1~2 分钟 |
| **③ VS Code** | 一次改很多、想调样式 | 装一个 VS Code | 推送后 1~2 分钟 |

三种改的是同一批文件，可以混着用。

---

## 方式一：写作台（推荐）

**双击 `写作台.bat`** → 浏览器会自动打开写作台 → 按页面上的四步走：

1. **左边选板块**（二次同人创作 / 数学分享 / 园艺记录 / 烹饪食谱 / 里版）
2. 点 **「＋ 写新的一篇」**，或点已有文章继续改
3. **上面几栏是"表单"**：标题、日期、摘要、标签……照着填；**下面的大框写正文**
4. 点 **「保存」**（只存到本地）或 **「保存并发布」**（推到线上，1~2 分钟后网站上就有了）

> **「保存并发布」会自动做三件事**：先在本地构建一次（有错就拦住，不把坏版本推上去）→
> 检查站内链接能不能点开 → 检查编辑器字段与网站模板是否一致 → 然后才推送。
> 结果会显示在窗口下方的日志里，所以"发出去才发现打不开"这种情况基本不会发生了。

### 正文里可以放心写的链接和图片

- 站内链接直接写 `/math/`、`/about/` 这种路径就行，网站会自动补上 `/Parlour/` 前缀（有一个"渲染钩子"在管这事）。
- 图片除了上面三种插入方式，也可以手写 `![说明](/images/xxx.png)`，同样会自动补前缀。
- 外链（`https://…`）会自动新窗口打开。

### 只有「标题」是必填的

其他都能留空：日期空着会自动填今天，摘要、标签、封面图都可以不写。
所以**只想快速记一笔的话，填个标题、写两行字、点「保存并发布」就行了**。

### 正文里插图片（三种方式任选）

| 方式 | 怎么做 |
| --- | --- |
| 点按钮 | 正文框上方的 **🖼 插入图片**，选文件 |
| 拖进来 | 直接把图片文件**拖进正文框** |
| 粘贴 | 截图（Win+Shift+S）后在正文框里按 **Ctrl+V** |

三种方式都会：把图片存进 `static/images/` → 在光标处插好 `![图片](/images/xxx.png)` → 网站上就显示成图了。
你不用管路径，也不用先上传到哪里。上传完下方会出现缩略图，点缩略图可以再插一次。

### 改 / 删以前的文章

- **改**：左边文章列表点一下，内容会填回表单，改完「保存并发布」。
- **删**：打开那篇文章 → 底部 **「删除这篇文章」** → 确认。
  文件会被挪到本机的 `.tools/trash/`（**不会真的丢**，想找回随时说），再点一次「保存并发布」，网站上就没有它了。

### 改首页 / 每个板块的介绍页 / 联系方式

顶部切到 **🏠 站点设置**，一共三块：

| 区块 | 对应网站上的哪里 | 能改什么 |
| --- | --- | --- |
| **① 首页文案** | 打开网站第一屏到「最近的记录」 | 名字、名字上方的小字、一行定位语、自我介绍、两个按钮的文字与链接；「关于我」区块的标题/照片/段落/速览；「最近的记录」的标题与显示条数 |
| **② 板块介绍页** | 点导航进某个板块后，那一页的大标题、副标题和列表上方的介绍文字 | 每个板块单独设置。大标题留空就用板块显示名，副标题留空就用一句话介绍 |
| **③ 联系方式** | 页脚最右边，以及「自我介绍」页里的联系方式 | 增删条目、换图标、改链接与显示文字 |

几点说明：

- **首页「关于我」里的照片**：在 ① 里点「上传」，图片会存进 `static/images/` 并自动填好路径；留空就显示字母方框。
- **段落**与**速览**是"一行一条"：速览写成 `名目 | 内容`（例如 `常驻 | 某座四季分明的城市`）。
- **板块的显示名**（导航上那几个字）和**首页卡片上的一句话介绍**，在 **🗂 板块管理** 里改；
  「② 板块介绍页」管的是点进去之后那一页的标题与正文。两者配合起来就是"导航叫什么 + 进去看到什么"。
- 里版介绍页里还有解谜题目（`gate`）等设置，写作台会**原样保留**，不会被覆盖。
- 改完记得回「✍ 写作」点一次 **保存并发布**。

> 还有一个地方写作台暂时没覆盖：**站点名（MAP）、副标题、建站年份**，它们在 `hugo.toml` 里。
> 想改告诉我，我顺手挪到写作台能编辑的地方。

### 改板块名字 / 新增 / 删除板块

顶部切到 **⚙ 板块管理**，那里是一张表：

| 能改什么 | 说明 |
| --- | --- |
| 显示名 | 顶部导航、首页卡片、列表页标题都用它（例如把「园艺记录」改成「园艺手记」） |
| 一句话介绍 | 首页卡片上的说明文字 |
| 颜色 | 这个板块的主色（卡片、列表页的点缀色） |
| 图标 | 从 24 个内置图标里挑一个 |
| 排序 | 数字越小越靠前，决定导航和首页卡片的顺序 |
| 导航 / 首页 | 勾掉「导航」就不出现在顶部菜单；勾掉「首页」就不出现在首页卡片墙 |
| 新增板块 | 填一个英文代号（例如 `notes`）+ 显示名即可，网址会是 `/notes/` |
| 删除板块 | 该板块的全部文章会挪到 `.tools/trash/`，网站上同时去掉导航与卡片 |

改完点每行的 **保存**，再回到「✍ 写作」点一次 **保存并发布**，网站就更新了。
新增的板块会自动写入 `data/sections.yaml`，并同步更新网页后台的配置（`.pages.yml`），所以手机端后台也能立刻看到新板块。

> 板块的**代号**（网址里那一段，如 `/fanworks/`）建好后就别改了——改了等于换了网址，旧链接会失效。
> 想改代号的话告诉我，我帮你连老链接一起处理。

### 其他要点

- **草稿**：新文章默认勾着「草稿」，意思是不对外显示。写满意了**取消勾选**再发布。
- **文件名决定网址**：建议填英文短名（例如 `rain-stops`）。填中文也能用，只是网址会变成一串 `%E9%9B%A8` 编码。
- **不会破坏已有文章**：写作台只改你正在编辑的那一篇；表单上没显示的设置也会原样保留（保存时会提示有哪些）。
- **需要 Node.js**：你电脑上已经装好了（写作台靠它运行）。换电脑的话去 <https://nodejs.org/> 装一个即可。
- 写作台**只在本机运行**（`127.0.0.1`），不对外网开放，别人访问不到。
- 关掉那个黑色窗口就等于关闭写作台。

**写作台起不来怎么办**

| 现象 | 原因 |
| --- | --- |
| 窗口一闪而过 / 提示找不到 node | 没装 Node.js → 去 <https://nodejs.org/> 装 |
| 浏览器没自动打开 | 手动打开 <http://127.0.0.1:4321/> |
| 提示"已经在运行了" | 之前那个窗口还开着，用它已经打开的页面即可 |
| 「保存并发布」失败 | 网络问题 → 窗口里会显示重试过程；也可以双击 `push.bat` 再试 |

---

## 方式二：网页后台（Pages CMS）

适合"人在外面、只有手机"的场景。注意**第一次登录需要浏览器能打开 github.com**（国内网络可能要多刷几次或换手机热点）。

1. 浏览器打开 **https://app.pagescms.org**
2. 用 GitHub 账号登录，第一次会让你授权本仓库（选 `MysteriousAmateurPlayer/Parlour`）
3. 左侧就是栏目：**二次同人创作 / 数学分享 / 园艺记录 / 烹饪食谱 / 里版 / 站点设置**
4. 进栏目 → `New` 新建（或点已有文章编辑）→ 填表单 → **Save**
5. 保存即自动提交到 GitHub，1~2 分钟后刷新网站就能看到

几个要点：

- **图片**：编辑器里直接上传，会自动存进 `static/images/` 并写好路径，不用自己算。
- **草稿**：新建时「草稿」默认是勾上的，写完了**取消勾选**才会对外的网站显示。
- **正文里的短代码**（下面会讲，例如 `{{< note >}}`）：请把编辑器切到 **Source / 源码** 模式再写，富文本模式可能会把符号转义掉。
- **首页文案、联系方式、自我介绍页** 都在「站点设置」里，改完直接生效。
- 后台只认 `.pages.yml` 里列出的字段，**不要手工删那个文件里的字段定义**（会导致保存时丢字段）。

---

## 方式三：VS Code（本地）

1. 装 [VS Code](https://code.visualstudio.com/)（免费），首次打开会提示安装推荐插件，点安装即可。
2. `文件 → 打开文件夹` → 选 `D:\个人网站`
3. **按 `Ctrl+Shift+B`** 启动本地预览，浏览器打开 http://localhost:1313/ ，改文件即时刷新。
4. 新建文章：`Ctrl+Shift+P` → 输入 `Run Task` → 选「新建一篇文章」→ 选板块、输入标题。
5. 写完后推送到 GitHub（`源代码管理` 面板：填一句说明 → 提交 → 同步），公网 1~2 分钟后更新。

顺手记两个快捷键：`Ctrl+Shift+V` 打开 Markdown 预览；`Ctrl+F5` 强制刷新浏览器。

---

## 每篇文章开头的「表单」（front matter）

文件最上面 `---` 之间那段就是 front matter。所有文章通用：

| 字段 | 说明 |
| --- | --- |
| `title` | 标题 |
| `date` | 日期，例如 `2026-09-20`（决定排序） |
| `description` | 摘要，显示在卡片、列表和搜索结果里 |
| `tags` | 标签，例如 `["短篇", "治愈"]`；写了就会自动生成标签页 |
| `draft` | `true` = 草稿，只有本地预览能看到 |
| `cover` | 封面图，例如 `/images/rose.jpg`；不写就用自动生成的编号色块 |
| `toc` | `true` = 宽屏显示右侧目录 |
| `private` | `true` = 不被搜索引擎收录（里版用） |

各板块额外字段：

| 板块 | 额外字段 |
| --- | --- |
| 数学 | `math: true` 才会渲染公式 |
| 园艺 | `weather`，例如 `"晴，22℃"`，显示在年表里 |
| 同人 | `source`（原作）、`series`（系列名） |
| 食谱 | `servings` 份量、`prep_time` 准备分钟、`cook_time` 烹饪分钟、`difficulty` 难度、`ingredients` 分组食材、`steps` 步骤列表 |
| 里版 | 必须写 `private: true` |

食谱的食材和步骤写在 front matter 里（正文用来写心得）：

```yaml
servings: 3
prep_time: 20
cook_time: 110
difficulty: "中等"
ingredients:
  - group: 主料
    items: ["牛腩 700g", "番茄 3 个"]
  - group: 调味
    items: ["生抽 2 大勺"]
steps:
  - "牛腩冷水下锅焯水，撇净浮沫。"
  - "番茄炒出沙，下牛腩翻炒。"
```

---

## 正文里能用的「短代码」

```markdown
{{< note type="tip" title="小提示" >}}内容支持 Markdown{{< /note >}}
{{< note title="注意" >}}type 还可以是 idea / warn{{< /note >}}

{{< theorem title="勾股定理" number="1.2" >}}设直角三角形……{{< /theorem >}}

{{< proof >}}由连分数的基本恒等式……{{< /proof >}}
```

## 数学公式

front matter 里写 `math: true`，正文直接写 LaTeX：

```markdown
行内公式 $e^{i\pi} + 1 = 0$

$$
\int_{-\infty}^{\infty} e^{-x^2}\,\mathrm{d}x = \sqrt{\pi}
$$
```

公式用的是本地 KaTeX（不联网也能渲染），只在这个页面加载，不影响其他页面的速度。

## 图片

- 放**静态图片**（照片、截图）：把文件丢进 `static/images/`，正文里写 `![说明](/images/文件名.jpg)`。
- 用网页后台时直接上传即可，路径它会自己写。
- 文件名建议用英文，路径**区分大小写**（`Rose.jpg` 和 `rose.jpg` 是两个文件）。

---

## 里版（需要解谜才能进的那间屋子）

- 题目、提示、输入框占位符在 `content/vault/_index.md` 的 `gate:` 段。
- **改答案**：

  ```powershell
  cd D:\个人网站
  powershell -ExecutionPolicy Bypass -File scripts\hash-answer.ps1 "你的新答案"
  ```

  把输出的那串哈希填到 `hugo.toml` 的 `params.vault.answerHash`。
  答案的大小写和空格不影响判定。
- ⚠️ **重要**：这是「软门禁」。静态网站的 HTML 是公开的，懂技术的人可以直接看到 `/vault/` 下的页面内容。
  所以里版适合放**还没准备好公开、但漏出去也不致命**的东西（废案、草稿、设定笔记）。
  想放真正私密的内容，告诉我，我给你做构建时加密或干脆不发布。

---

## 改网站的其他部分

| 想改什么 | 改哪里 | 网页后台能改吗 |
| --- | --- | --- |
| 首页大字、定位语、自我介绍、按钮 | `data/home.yaml` | ✅「站点设置 → 首页文案」 |
| 联系方式（邮件、GitHub、B站……） | `data/socials.yaml` | ✅「站点设置 → 联系方式」 |
| 自我介绍页正文 | `content/about/_index.md` | ✅「站点设置 → 自我介绍页正文」 |
| 版块名称、编号、主色、图标 | `data/sections.yaml` | ❌ 用 VS Code 改（一次性设置） |
| 站点名、副标题、导航菜单、建站年份 | `hugo.toml` | ❌ 用 VS Code 改 |
| 里版题目与答案 | `content/vault/_index.md` + `hugo.toml` | ❌ 用 VS Code 改 |

> `hugo.toml` 里有很多中文注释。**不要**用网页后台或在线的 TOML 编辑器改它——那些工具会把注释全部抹掉。
> `data/sections.yaml` 我故意没有放进网页后台，就是为了避免在表单里误删 `accent`（主色）这类字段。

---

## 改完怎么确认

1. **本地**：`Ctrl+Shift+B` 看 http://localhost:1313/
2. **公网**：打开 GitHub 仓库 → `Actions` 标签页
   - 🟢 绿色勾 = 发布成功（点进去能看到耗时 1~2 分钟）
   - 🔴 红叉 = 构建失败，点开红色那一步把日志发我，基本都能修
3. **终极检查**（本地）：

   ```powershell
   cd D:\个人网站
   .\build.bat
   node scripts\verify.cjs
   ```

   它会检查首页五个板块、公式是否只在数学页加载、食谱数据是否合法等等。

---

## 常见问题

| 现象 | 多半是因为 |
| --- | --- |
| 改了内容网站没变化 | 草稿没取消勾选 / 忘了保存 / 部署还在跑（等 1~2 分钟）/ 浏览器缓存（`Ctrl+F5`） |
| 公式显示成 `$$...$$` 原文 | front matter 少了 `math: true` |
| 图片不显示 | 路径没以 `/images/` 开头，或大小写不对 |
| 文章没出现在列表里 | `draft: true` 没改，或 `date` 写成了将来的时间 |
| 网址变成一串 `%E9%9B%A8...` | 文件用了中文名。新建时把文件名写成英文（如 `rain-stops`）即可，功能不受影响 |
| 标签页/系列页是空的 | 标签写在 `tags:` 里才会生成，注意是数组写法 `["短篇", "治愈"]` |
