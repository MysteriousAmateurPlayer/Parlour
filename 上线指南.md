# 上线指南 · 从零到 https://你的名字.github.io/仓库名/

需要的东西：一个邮箱、这个文件夹（`D:\个人网站`，代码和自动部署配置已经准备好了）。
大概 20 分钟，之后每次更新都是自动的。

---

## 第一步：注册 GitHub

1. 打开 **https://github.com/signup**
2. 填邮箱、密码、用户名。**用户名要想好再用**——它会直接出现在你的网址里，
   比如 `arcarlight` → `https://arcarlight.github.io/`。
   - 只能用英文字母、数字和连字符 `-`，不能有中文和空格
   - 定下来之后改起来很麻烦（旧网址会失效），建议用能长期用的名字
3. 去邮箱点验证链接（收不到就看垃圾邮件）
4. 免费账号就够用。GitHub Pages 对**公开仓库**免费。

> 如果打不开 github.com，属于网络问题，可以稍后再试或换个网络环境。

---

## 第二步：建一个仓库（repository）

1. 登录后，右上角 `+` → **New repository**
2. **Repository name** 填仓库名，例如 `BlogStand`（也会出现在网址里）
3. 选 **Public**（公开）——免费版的 GitHub Pages 需要公开仓库
4. 下面三个勾（Add README / .gitignore / license）**都不要勾**，我们本地已经有了
5. 点 **Create repository**
6. 建好后页面上会给出仓库地址，形如：
   `https://github.com/你的用户名/BlogStand.git`

---

## 第三步：开启 Pages（顺序很重要，先做这步）

**请在推送代码之前做这一步**，否则第一次自动部署会在「Setup Pages」这步失败
（那时的报错是 `Get Pages site failed`，不是你的代码有问题）。

1. 进入仓库 → **Settings** → 左侧 **Pages**
2. `Build and deployment` → **Source** 选 **GitHub Actions**
   （**不要**选 "Deploy from a branch"）
3. 做完再去第四步推送代码

如果已经先推送了、Actions 里出现红叉：打开失败的那次运行 → 右上角 **Re-run all jobs** 即可。

---

## 第四步：把本地代码推上去

### 方式 A：GitHub Desktop（图形界面，推荐新手）

1. 下载安装 **https://desktop.github.com** ，打开后登录 GitHub 账号
2. 菜单 `File → Add local repository…` → 选 `D:\个人网站` → Add
3. 右上角点 **Publish repository**
   - 取消勾选 `Keep this code private`
   - 仓库名可以改，然后点 **Publish repository**
4. 传完后本地和 GitHub 就同步了。

**以后每次改完内容**：左下角填一句说明 → `Commit to main` → 右上角 `Push origin`。

> 国内网络下 GitHub Desktop 常常登不上或推不上去。
> 如果遇到，直接跳到后面的「**★ 国内网络受限怎么办**」一节，用 `push.bat` 走 SSH 443 推送。

### 方式 B：命令行（项目里已经 `git init` 过了）

```powershell
cd D:\个人网站
git remote add origin https://github.com/你的用户名/BlogStand.git
git branch -M main
git push -u origin main
```

推送时会要密码——GitHub 现在不能用账号密码，要用 **Personal Access Token**：
`GitHub → 右上角头像 → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token (classic)`，
勾上 `repo` 权限，生成后复制那串 `ghp_...`，在提示 `Password:` 时粘贴它（只显示一次，记得存好）。

> 顺带一提：现在的 git 提交身份是 `MAP <...@noreply.cnb.cool>`。
> 想让 GitHub 把提交算到你账号上，可以改成 GitHub 给你的匿名邮箱：
> `git config --global user.email "你的ID+用户名@users.noreply.github.com"`（在 GitHub 邮箱设置里能找到）。

---

## 第五步：看它自动部署

1. 推送完成后，回到仓库首页 → **Actions** 标签页
2. 应该能看到 **Deploy Hugo site to Pages** 正在跑（黄色圆点），等 1~2 分钟变成**绿色勾**
3. 访问：

   ```
   https://你的用户名.github.io/仓库名/
   ```

   例如 `https://arcarlight.github.io/BlogStand/`

4. 绿色勾之后可能还要等一两分钟才能访问，稍等再刷新。

> 部署配置在 `.github/workflows/hugo.yml`，它会自动装上 Hugo、构建站点、
> 并且**自动把网址（baseURL）设置成你的实际地址**，所以不需要手工改配置。

---

## 第六步：体检（推荐跑一次）

拿到网址后，在本地跑一遍自动体检——它会逐个打开首页、五个版块、文章、RSS、
站点地图、KaTeX 字体，并且专门检查新手最容易踩的「CSS/JS 404 导致页面没样式」：

```powershell
cd D:\个人网站
node scripts\check-live.cjs https://你的用户名.github.io/仓库名/
```

全绿就说明真的上线成功了。也可以把网址发我，我帮你跑并看结果。

手动检查清单：

- [ ] 首页能打开，大标题是 MAP
- [ ] 明暗切换按钮能用（右上角太阳/月亮）
- [ ] 五个版块都能点进去，文章能打开
- [ ] 手机浏览器打开也正常
- [ ] `/vault/` 那道解谜题能出现

有问题不要自己硬试，把现象（或 Actions 里的红色报错、体检脚本的输出）发我，一般一两下就能修好。

---

## ★ 国内网络受限怎么办（github.com 打不开 / 推送失败）

这是国内常见情况，**不是你的操作问题**。我在你这台机器上实测过，结论是：

| 目标 | 实测结果 | 说明 |
| --- | --- | --- |
| `github.com`（网页、HTTPS 推送） | ❌ 连续 6 次全部超时 | 间歇性干扰：偶尔能通，但没法稳定用 |
| `ssh.github.com:443` | ✅ 通（142ms，返回 SSH 握手） | **GitHub 官方给受干扰网络留的推送通道** |
| `app.pagescms.org`（网页写作后台） | ✅ HTTP 200 | 以后写文章走它，提交是它服务器做的 |
| `*.github.io`（你的站点域名） | ✅ 通（证书 `*.github.io` 有效） | 部署后你和朋友都能打开 |

所以路线是：**网页操作尽量少，真正的推送走 SSH 的 443 端口。**

### 第 1 步：把 SSH 公钥加到 GitHub（一次性）

公钥已经在你电脑上生成好了，就是下面这一行（**可以公开，不是秘密**）：

```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPqhyaB7I4ZZ9i9+Fc3KH4mAcSun57+bin722TaXFxq5 MysteriousAmateurPlayer@users.noreply.github.com
```

操作：打开 <https://github.com/settings/keys> → **New SSH key** → Title 随便写（例如 `家里的电脑`）→
Key 里粘贴上面整行 → **Add SSH key**。

> 打不开这个页面怎么办（按成功率排序）：
> 1. **多刷新几次**——干扰是间歇性的，常常刷两三次就出来了；
> 2. **用手机热点**给电脑上网，换一条出口线路往往立刻就好；
> 3. **用手机浏览器**登录 GitHub 操作（手机上通常比宽带稳）；
> 4. 装一个 GitHub 加速工具（例如 Watt Toolkit / dev-sidecar），它们本质是帮你换可用 IP。

### 第 2 步：建仓库 + 开启 Pages

跟前面第二、三步一样，但同样可能要多刷几次：

- 建仓库：<https://github.com/new> → 名字 **`Parlour`** → **Public** → 不勾任何初始化选项
- 开 Pages：仓库 `Settings → Pages → Source = GitHub Actions`

### 第 3 步：推送（走 443，自动重试）

```powershell
cd D:\个人网站
.\push.bat
```

或者 `powershell -ExecutionPolicy Bypass -File scripts\push.ps1`。

这个脚本会：先检查 SSH 通道（不通就明确告诉你原因）→ 自动提交未提交的改动 →
推送并**失败自动重试 5 次**。它用的是 `ssh.github.com:443`，不受 `github.com:443` 干扰影响。

> 我已经把这台电脑的 SSH 配置好了（`C:\Users\15201\.ssh\config`），
> 让 `github.com` 自动指向 `ssh.github.com:443`，并且校验过主机密钥指纹与
> GitHub 官方公布的一致（`SHA256:+DiY3wvvV6TuJJhbpZisF/zLDA0zPMSvHdkr4UvCOqU`），不存在中间人风险。

### 以后的日常更新

推上去一次之后，**写文章根本不需要碰 GitHub 网页**：

打开 <https://app.pagescms.org>（实测能开）→ 选你的仓库 → 改内容 → Save。
提交是 Pages CMS 的服务器替你做的，和你本地网络无关；GitHub Actions 会自动重新发布。

### 保底方案：完全绕开 GitHub

如果哪天 GitHub 网页彻底上不去，也可以不用它：

1. 本地双击 `build.bat` 生成 `public/` 文件夹
2. 注册腾讯云 [EdgeOne Pages](https://edgeone.ai/zh/pages)（实测国内可达），
   在控制台里**直接上传 `public/` 文件夹**，它会给你一个国内访问很快的网址
3. 这个方案不需要 GitHub 账号，代价是不能用 Pages CMS 那种"保存即发布"的后台

需要我帮你改成 EdgeOne / Cloudflare Pages 的部署方式，或者把站点同时发布到两个平台
（GitHub 主站 + 国内镜像），说一声即可。

---

## 以后怎么更新

| 你做了什么 | 会发生什么 |
| --- | --- |
| 网页后台（Pages CMS）点了保存 | 自动提交 → Actions 自动重新发布 → 1~2 分钟后网站上就有了 |
| 本地 VS Code 写完 → 提交 → 同步 | 同上 |
| 只在本地点了保存、没 push | 只有本地预览变，公网不变 |

去哪看发布状态：仓库 → **Actions**。绿色勾 = 成功。

---

## 常见问题

| 现象 | 原因 / 解决 |
| --- | --- |
| 访问网址是 404 | Pages 的 Source 没选成 GitHub Actions；或网址里用户名/仓库名拼错了（区分大小写） |
| Actions 是红叉 | 点进去看哪一步失败，把日志发我。多半是 Hugo 版本或某个页面的模板报错 |
| 网站能开但没样式 | 极少见，一般是 baseURL 问题；告诉我，我改一行就好 |
| 链接点进去 404 | 同上；也可能是文章还是草稿状态 |
| 打开很慢 / 时好时坏 | GitHub Pages 的服务器在境外，国内访问不稳定。想快可以换 Cloudflare Pages（见下） |
| 想改成自己的域名 | Pages 页面里填 `Custom domain`，再去域名商加一条 CNAME 记录指向 `你的用户名.github.io` |
| 不想让它被搜索引擎搜到 | 告诉我，我给你加一个"全站 noindex"开关（里版本身已经是 noindex） |

---

## 可选：国内访问更快 / 自定义域名

**Cloudflare Pages**（免费、国内通常比 GitHub Pages 快，也支持绑定自己的域名）：

1. 注册 https://dash.cloudflare.com
2. `Workers & Pages → Create → Pages → Connect to Git`，授权并选中这个仓库
3. 构建设置：
   - Framework preset: `Hugo`
   - Build command: `hugo --minify --gc`
   - Build output directory: `public`
   - 环境变量：`HUGO_VERSION = 0.166.0`
4. 保存后它会给一个 `xxx.pages.dev` 的网址，绑定自己的域名也是在这里配

需要我按 Cloudflare 或 Vercel 再写一份细化步骤，说一声即可。

---

## 关于公开仓库这件事

仓库是 Public，意味着**你的文章源文件也公开可见**（和网站内容一样，不是额外泄露）。

需要注意的只有一处：**里版**。它是"软门禁"——网站上的门禁要靠输入答案才能看到内容，
但懂技术的人可以直接在仓库或网页源码里找到 `/vault/` 下的文章。
所以请把里版当成"给朋友的暗门"，而不是"保险柜"：
放废案、草稿、设定笔记可以；放真正的隐私信息（住址、证件、别人的隐私）不行。

想放真正私密的东西，两个办法（我都可以帮你做）：

1. **构建时加密**：里版内容在发布前被 AES 加密，只有输入口令的浏览器能解开；
2. **干脆不发布**：里版留在本地（或用私有仓库），公开站点里只留一个提示页。
